{
  "name": "dployr-base",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "build": "pnpm dlx esbuild src/index.unified.ts --bundle --platform=node --format=esm --tsconfig=tsconfig.json --outdir=dist --external:@hono/node-server --external:smol-toml --external:redis --external:@upstash/redis --external:@aws-sdk/client-s3"
  },
  "dependencies": {
    "@octokit/auth-app": "^8.1.2",
    "@octokit/rest": "^22.0.1",
    "buffer": "^6.0.3",
    "hono": "^4.8.4",
    "jose": "^6.1.2",
    "ulid": "^3.0.1",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250712.0",
    "wrangler": "^4.4.0"
  }
}